{
  "name": "myappart-functions",
  "version": "1.0.0",
  "description": "Netlify Functions — MyAppart API layer",
  "scripts": {
    "dev": "cd client && npm run dev",
    "build": "cd client && npm ci && npm run build"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "ws": "^8.19.0"
  }
}
